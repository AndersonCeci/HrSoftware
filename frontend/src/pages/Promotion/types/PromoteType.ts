export type PromoteType = {
  _id: string;
  name: string;
  surname: string;
  oldPosition: string;
  newPosition: string;
  newSalary: string;
  oldSalary: string;
  trainedBy: string;
  dateOfPromotion: string;
};
